/*
███████████████████████████████████████████████████████████████████████████████████████
█                                                                                     █
█   📘 JAVA DSA CHEAT SHEET: ARRAYS & STRINGS                                        █
█   From Scratch to Advanced - Complete Study Guide                                  █
█                                                                                     █
███████████████████████████████████████████████████████████████████████████████████████


═══════════════════════════════════════════════════════════════════════════════════════
│                          🔹 PART 1: ARRAY FUNDAMENTALS                              │
═══════════════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 1. What is an Array?                                                                │
└─────────────────────────────────────────────────────────────────────────────────────┘
   📌 Definition:
      - Linear data structure storing homogeneous elements (same data type)
      - Elements are stored in contiguous memory locations
      - Allows random access using an index
      - Access time complexity: O(1)
   
   📌 Key Points:
      ✓ Fast access to elements using index
      ✓ Fixed size after creation
      ✓ Memory efficient for large datasets
      ✗ Insertion/Deletion in middle is costly (O(n))


┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 2. Why are Arrays 0-Indexed?                                                        │
└─────────────────────────────────────────────────────────────────────────────────────┘
   📌 Reason:
      - Simplifies memory address calculation
      - Formula: address = base_address + (index * size_of_element)
      - For size 'n', valid indices are 0 to n-1
   
   📌 Example:
      If base_address = 1000, element_size = 4 bytes
      - Index 0: 1000 + (0 * 4) = 1000
      - Index 1: 1000 + (1 * 4) = 1004
      - Index 2: 1000 + (2 * 4) = 1008


┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 3. Declaring & Creating Arrays                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
   📌 Syntax:
      datatype[] arrayName = new datatype[size];
   
   📌 Examples:
      int[] arr = new int[5];           // Creates array with default values
      String[] names = new String[10];  // Array of String objects
      double[] prices = new double[3];  // Array of doubles


┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 4. Default Values in Java                                                           │
└─────────────────────────────────────────────────────────────────────────────────────┘
   📌 Primitive Types:
      int      → 0
      double   → 0.0
      boolean  → false
      char     → '\u0000' (null character)
   
   📌 Reference Types:
      String   → null
      Object   → null


┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 5. Initialization Methods                                                           │
└─────────────────────────────────────────────────────────────────────────────────────┘
   📌 Method 1: Direct Initialization
      int[] arr = {10, 20, 30, 40};
   
   📌 Method 2: Using 'new' Keyword
      int[] arr = new int[]{10, 20, 30, 40};
   
   📌 Method 3: Index-wise Assignment
      int[] arr = new int[4];
      arr[0] = 10;
      arr[1] = 20;
      arr[2] = 30;
      arr[3] = 40;


┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 6. Traversing an Array                                                              │
└─────────────────────────────────────────────────────────────────────────────────────┘
   📌 Method 1: Standard For Loop
      for(int i = 0; i < arr.length; i++) {
          System.out.println(arr[i]);
      }
   
   📌 Method 2: Enhanced For Loop (For-Each)
      for(int x : arr) {
          System.out.println(x);
      }
   
   📌 When to Use Which?
      ✓ Use standard loop when you need index
      ✓ Use enhanced loop when you only need values


┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 7. Searching & Operations                                                           │
└─────────────────────────────────────────────────────────────────────────────────────┘
   📌 Linear Search: O(n)
      - Search element one by one
      - Works on unsorted arrays
   
   📌 Binary Search: O(log n)
      - Only works on SORTED arrays
      - Divides search space in half each time
   
   📌 Insertion/Deletion (Middle): O(n)
      - Requires shifting elements
      - Costly operation in arrays



═══════════════════════════════════════════════════════════════════════════════════════
│                        🔹 PART 2: STRING FUNDAMENTALS                               │
═══════════════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 1. What is a String?                                                                │
└─────────────────────────────────────────────────────────────────────────────────────┘
   📌 Definition:
      - Sequence of characters stored as an Object
      - Not a primitive data type in Java
      - Internally stored as char array
   
   📌 Key Characteristic:
      ⚠️ STRINGS ARE IMMUTABLE IN JAVA
      - Once created, cannot be changed
      - Any modification creates a new String object


┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 2. String Creation Methods                                                          │
└─────────────────────────────────────────────────────────────────────────────────────┘
   📌 Method 1: String Literal (String Pool)
      String s = "striver";
      - Stored in String Pool (special memory area)
      - Reuses existing string if found in pool
   
   📌 Method 2: Using 'new' Keyword (Heap)
      String s = new String("striver");
      - Creates new object in Heap memory
      - Even if "striver" exists in pool


┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 3. Important String Methods                                                         │
└─────────────────────────────────────────────────────────────────────────────────────┘
   📌 Access Methods:
      s.charAt(index)      → Get character at specific index
      s.length()           → Returns number of characters
      
   📌 Comparison Methods:
      s.equals(s2)         → Compare values (ALWAYS USE THIS)
      s.equalsIgnoreCase() → Case-insensitive comparison
      s.compareTo(s2)      → Lexicographical comparison
      
   📌 Search Methods:
      s.indexOf(char)      → Find first occurrence
      s.lastIndexOf(char)  → Find last occurrence
      s.contains(str)      → Check if substring exists
      
   📌 Modification Methods (Create New String):
      s.substring(start)   → Extract substring
      s.replace(old, new)  → Replace characters
      s.toLowerCase()      → Convert to lowercase
      s.toUpperCase()      → Convert to uppercase
      s.trim()             → Remove leading/trailing spaces


┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 4. String Comparison: equals() vs ==                                                │
└─────────────────────────────────────────────────────────────────────────────────────┘
   ⚠️ CRITICAL INTERVIEW POINT:
   
   📌 Using == (WRONG for value comparison):
      String s1 = new String("hello");
      String s2 = new String("hello");
      System.out.println(s1 == s2);        // false (compares memory address)
   
   📌 Using .equals() (CORRECT):
      System.out.println(s1.equals(s2));   // true (compares actual values)
   
   📌 Rule:
      ✓ ALWAYS use .equals() for String value comparison
      ✗ == compares memory addresses (reference comparison)


┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 5. Concatenation & StringBuilder                                                    │
└─────────────────────────────────────────────────────────────────────────────────────┘
   📌 Method 1: Using '+' Operator
      String a = "Hello" + "World";
      ⚠️ Problem: Creates new objects each time (slow in loops)
      
      // BAD PRACTICE (in loops):
      String result = "";
      for(int i = 0; i < 1000; i++) {
          result += i;  // Creates 1000 new String objects!
      }
   
   📌 Method 2: StringBuilder (BEST for DSA) ⭐
      StringBuilder sb = new StringBuilder();
      sb.append("Hello");
      sb.append("World");
      String result = sb.toString();
      
      ✓ Mutable (can be changed)
      ✓ Faster performance
      ✓ Less memory usage
      
      // GOOD PRACTICE:
      StringBuilder result = new StringBuilder();
      for(int i = 0; i < 1000; i++) {
          result.append(i);  // Efficient!
      }
   
   📌 Important StringBuilder Methods:
      sb.append(str)       → Add to end
      sb.insert(pos, str)  → Insert at position
      sb.delete(start,end) → Remove substring
      sb.reverse()         → Reverse the string
      sb.toString()        → Convert to String



═══════════════════════════════════════════════════════════════════════════════════════
│                   🔹 PART 3: ARRAYS VS STRINGS COMPARISON                           │
═══════════════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────────────┐
│ Feature Comparison Table                                                            │
└─────────────────────────────────────────────────────────────────────────────────────┘

   Feature          │ Array                          │ String
   ─────────────────┼────────────────────────────────┼─────────────────────────────
   Size             │ Fixed after creation           │ Fixed after creation
   Mutability       │ Mutable (arr[0] = 5)          │ Immutable
   Storage          │ Primitives or Objects          │ Objects only
   Length Property  │ arr.length (field)            │ s.length() (method)
   Memory           │ Contiguous allocation          │ String Pool + Heap
   Modification     │ arr[i] = value (direct)       │ Creates new object

   📌 Key Takeaway:
      - Arrays: Use when you need to modify elements frequently
      - Strings: Use for text; use StringBuilder for modifications



═══════════════════════════════════════════════════════════════════════════════════════
│          ✅ PART 4: CORE DSA IMPLEMENTATIONS WITH CODE                              │
═══════════════════════════════════════════════════════════════════════════════════════
*/

import java.util.*;


// ╔═══════════════════════════════════════════════════════════════════════════════════╗
// ║                     1️⃣ 2D ARRAYS (MATRIX PROBLEMS)                               ║
// ╚═══════════════════════════════════════════════════════════════════════════════════╝

/*
┌─────────────────────────────────────────────────────────────────────────────────────┐
│ 2D Array Basics                                                                     │
└─────────────────────────────────────────────────────────────────────────────────────┘
   📌 What is a 2D Array?
      - "Array of arrays"
      - Represents data in rows and columns (like a table/matrix)
      
   📌 Declaration:
      int[][] matrix = new int[rows][cols];
      
   📌 Initialization:
      int[][] mat = {
          {1, 2, 3},
          {4, 5, 6},
          {7, 8, 9}
      };
   
   📌 Accessing Elements:
      mat[row][col]  → Access element at row, col
      mat.length     → Number of rows
      mat[0].length  → Number of columns
*/

class MatrixDSA {
    
    /*
    ┌─────────────────────────────────────────────────────────────────────────────────┐
    │ PRINT MATRIX                                                                    │
    ├─────────────────────────────────────────────────────────────────────────────────┤
    │ Time Complexity: O(n * m) where n=rows, m=cols                                 │
    │ Space Complexity: O(1)                                                          │
    │                                                                                 │
    │ Logic:                                                                          │
    │ - Use nested loop to traverse all elements                                     │
    │ - Outer loop for rows, inner loop for columns                                  │
    └─────────────────────────────────────────────────────────────────────────────────┘
    */
    static void printMatrix(int[][] mat) {
        for (int i = 0; i < mat.length; i++) {           // Iterate through rows
            for (int j = 0; j < mat[0].length; j++) {    // Iterate through columns
                System.out.print(mat[i][j] + " ");
            }
            System.out.println();                         // New line after each row
        }
    }
    
    
    /*
    ┌─────────────────────────────────────────────────────────────────────────────────┐
    │ TRANSPOSE MATRIX                                                                │
    ├─────────────────────────────────────────────────────────────────────────────────┤
    │ Time Complexity: O(n * m)                                                       │
    │ Space Complexity: O(n * m) for result matrix                                   │
    │                                                                                 │
    │ Concept:                                                                        │
    │ - Swap rows and columns                                                         │
    │ - Original: mat[i][j] → Transposed: res[j][i]                                  │
    │                                                                                 │
    │ Example:                                                                        │
    │   Original:  [1 2 3]        Transposed:  [1 4 7]                               │
    │              [4 5 6]                      [2 5 8]                               │
    │              [7 8 9]                      [3 6 9]                               │
    └─────────────────────────────────────────────────────────────────────────────────┘
    */
    static int[][] transpose(int[][] mat) {
        int r = mat.length;        // Number of rows
        int c = mat[0].length;     // Number of columns
        int[][] res = new int[c][r]; // Result matrix (cols x rows)
        
        for (int i = 0; i < r; i++) {
            for (int j = 0; j < c; j++) {
                res[j][i] = mat[i][j];  // Swap row and column indices
            }
        }
        return res;
    }
    
    
    /*
    ┌─────────────────────────────────────────────────────────────────────────────────┐
    │ MATRIX SUM                                                                      │
    ├─────────────────────────────────────────────────────────────────────────────────┤
    │ Time Complexity: O(n * m)                                                       │
    │ Space Complexity: O(1)                                                          │
    │                                                                                 │
    │ Logic:                                                                          │
    │ - Use enhanced for loop to traverse all elements                               │
    │ - Add each element to sum                                                       │
    └─────────────────────────────────────────────────────────────────────────────────┘
    */
    static int sumMatrix(int[][] mat) {
        int sum = 0;
        for (int[] row : mat)           // For each row
            for (int val : row)          // For each value in row
                sum += val;              // Add to sum
        return sum;
    }
}


// ╔═══════════════════════════════════════════════════════════════════════════════════╗
// ║                  2️⃣ STRING DSA (ANAGRAM + FREQUENCY)                             ║
// ╚═══════════════════════════════════════════════════════════════════════════════════╝

/*
┌─────────────────────────────────────────────────────────────────────────────────────┐
│ String Problem Patterns                                                             │
└─────────────────────────────────────────────────────────────────────────────────────┘
   📌 Common Techniques:
      1. Frequency Array (for counting characters)
      2. Two Pointer (for palindromes, reversing)
      3. Sliding Window (for substrings)
      4. HashMap (for complex frequency problems)
*/

class StringDSA {
    
    /*
    ┌─────────────────────────────────────────────────────────────────────────────────┐
    │ CHECK ANAGRAM                                                                   │
    ├─────────────────────────────────────────────────────────────────────────────────┤
    │ Time Complexity: O(n) where n = length of string                               │
    │ Space Complexity: O(1) - frequency array size is constant (26)                 │
    │                                                                                 │
    │ Concept:                                                                        │
    │ - Anagrams have same characters with same frequency                            │
    │ - Example: "listen" and "silent" are anagrams                                  │
    │                                                                                 │
    │ Approach:                                                                       │
    │ 1. Use frequency array of size 26 (for lowercase a-z)                          │
    │ 2. Increment count for characters in first string                              │
    │ 3. Decrement count for characters in second string                             │
    │ 4. If all counts are 0, strings are anagrams                                   │
    │                                                                                 │
    │ Trick: s.charAt(i) - 'a' gives index (0-25) for lowercase letters             │
    └─────────────────────────────────────────────────────────────────────────────────┘
    */
    static boolean isAnagram(String s, String t) {
        // If lengths differ, cannot be anagrams
        if (s.length() != t.length()) return false;
        
        // Frequency array for 26 lowercase letters
        int[] freq = new int[26];
        
        // Count frequencies
        for (int i = 0; i < s.length(); i++) {
            freq[s.charAt(i) - 'a']++;  // Increment for s
            freq[t.charAt(i) - 'a']--;  // Decrement for t
        }
        
        // Check if all frequencies are zero
        for (int x : freq) 
            if (x != 0) return false;
        
        return true;
    }
    
    
    /*
    ┌─────────────────────────────────────────────────────────────────────────────────┐
    │ CHARACTER FREQUENCY                                                             │
    ├─────────────────────────────────────────────────────────────────────────────────┤
    │ Time Complexity: O(n + 256) ≈ O(n)                                              │
    │ Space Complexity: O(256) = O(1) - constant space                               │
    │                                                                                 │
    │ Concept:                                                                        │
    │ - Count frequency of each character in string                                  │
    │ - Use ASCII values (0-255) as indices                                          │
    │                                                                                 │
    │ Why size 256?                                                                   │
    │ - ASCII has 256 characters (0-255)                                             │
    │ - Covers all standard characters                                               │
    │                                                                                 │
    │ Alternative: Use HashMap for better space efficiency                           │
    └─────────────────────────────────────────────────────────────────────────────────┘
    */
    static void charFrequency(String s) {
        // Frequency array for all ASCII characters
        int[] freq = new int[256];
        
        // Count frequency of each character
        for (char ch : s.toCharArray()) {
            freq[ch]++;  // Use ASCII value as index
        }
        
        // Print non-zero frequencies
        for (int i = 0; i < 256; i++) {
            if (freq[i] > 0) {
                System.out.println((char) i + " -> " + freq[i]);
            }
        }
    }
}


// ╔═══════════════════════════════════════════════════════════════════════════════════╗
// ║              3️⃣ SLIDING WINDOW 🔥 (OPTIMIZATION TECHNIQUE)                       ║
// ╚═══════════════════════════════════════════════════════════════════════════════════╝

/*
┌─────────────────────────────────────────────────────────────────────────────────────┐
│ Sliding Window Technique                                                            │
└─────────────────────────────────────────────────────────────────────────────────────┘
   📌 What is Sliding Window?
      - Optimization technique for array/string problems
      - Reduces time complexity from O(n²) to O(n)
      - Works on contiguous subarrays/substrings
   
   📌 Two Types:
      1. Fixed Window Size
         - Window size is constant (e.g., size k)
         - Example: Max sum of k consecutive elements
      
      2. Variable Window Size
         - Window size changes dynamically
         - Example: Longest substring without repeating characters
   
   📌 How it Works:
      - Expand window by moving right pointer
      - Shrink window by moving left pointer
      - Track answer during expansion/shrinkage
   
   📌 When to Use:
      ✓ Problems involving contiguous subarrays
      ✓ "Maximum/Minimum of size k" problems
      ✓ "Longest/Shortest substring with condition" problems
*/

class SlidingWindowDSA {
    
    /*
    ┌─────────────────────────────────────────────────────────────────────────────────┐
    │ MAX SUM SUBARRAY OF SIZE K (Fixed Window)                                      │
    ├─────────────────────────────────────────────────────────────────────────────────┤
    │ Time Complexity: O(n)                                                           │
    │ Space Complexity: O(1)                                                          │
    │                                                                                 │
    │ Problem: Find maximum sum of k consecutive elements                            │
    │                                                                                 │
    │ Brute Force Approach: O(n*k)                                                    │
    │ - For each position, calculate sum of k elements                               │
    │                                                                                 │
    │ Sliding Window Approach: O(n) ⭐                                                │
    │ 1. Calculate sum of first k elements                                           │
    │ 2. Slide window: Add next element, Remove first element                        │
    │ 3. Keep track of maximum sum                                                    │
    │                                                                                 │
    │ Example: arr = [1,3,2,6,1], k = 3                                              │
    │   Window 1: [1,3,2] = 6                                                         │
    │   Window 2: [3,2,6] = 11 (removed 1, added 6)                                  │
    │   Window 3: [2,6,1] = 9  (removed 3, added 1)                                  │
    │   Answer: 11                                                                    │
    └─────────────────────────────────────────────────────────────────────────────────┘
    */
    static int maxSumSubarray(int[] arr, int k) {
        // Step 1: Calculate sum of first k elements
        int sum = 0;
        for (int i = 0; i < k; i++) {
            sum += arr[i];
        }
        
        int maxSum = sum;  // Initialize max with first window
        
        // Step 2: Slide the window
        for (int i = k; i < arr.length; i++) {
            sum += arr[i] - arr[i - k];  // Add new element, remove old
            maxSum = Math.max(maxSum, sum);
        }
        
        return maxSum;
    }
    
    
    /*
    ┌─────────────────────────────────────────────────────────────────────────────────┐
    │ LONGEST SUBSTRING WITHOUT REPEATING CHARACTERS (Variable Window)               │
    ├─────────────────────────────────────────────────────────────────────────────────┤
    │ Time Complexity: O(n)                                                           │
    │ Space Complexity: O(256) = O(1) for seen array                                 │
    │                                                                                 │
    │ Problem: Find length of longest substring with unique characters               │
    │                                                                                 │
    │ Approach:                                                                       │
    │ - Use two pointers: left (l) and right (r)                                     │
    │ - Expand window by moving right pointer                                        │
    │ - If duplicate found, shrink from left until duplicate removed                 │
    │ - Track maximum length                                                          │
    │                                                                                 │
    │ Example: s = "abcabcbb"                                                         │
    │   - "abc" = 3 (unique)                                                          │
    │   - "abca" has duplicate 'a', shrink to "bca"                                  │
    │   - Continue... longest = 3                                                     │
    │                                                                                 │
    │ Key Insight:                                                                    │
    │ - boolean[] seen tracks if character is in current window                      │
    │ - While duplicate exists, remove from left                                     │
    └─────────────────────────────────────────────────────────────────────────────────┘
    */
    static int longestUniqueSubstring(String s) {
        boolean[] seen = new boolean[256];  // Track characters in window
        int l = 0;          // Left pointer
        int maxLen = 0;     // Maximum length found
        
        // Expand window with right pointer
        for (int r = 0; r < s.length(); r++) {
            // Shrink window if duplicate found
            while (seen[s.charAt(r)]) {
                seen[s.charAt(l)] = false;  // Remove from window
                l++;                         // Shrink from left
            }
            
            seen[s.charAt(r)] = true;        // Add current char to window
            maxLen = Math.max(maxLen, r - l + 1);  // Update max length
        }
        
        return maxLen;
    }
}


// ╔═══════════════════════════════════════════════════════════════════════════════════╗
// ║                      4️⃣ HASHING (FAST LOOKUP O(1))                               ║
// ╚═══════════════════════════════════════════════════════════════════════════════════╝

/*
┌─────────────────────────────────────────────────────────────────────────────────────┐
│ Hashing Fundamentals                                                                │
└─────────────────────────────────────────────────────────────────────────────────────┘
   📌 What is Hashing?
      - Data structure that maps keys to values
      - Provides O(1) average time for insert, delete, search
      
   📌 Java Hash Collections:
      1. HashMap<K, V>
         - Stores key-value pairs
         - No order maintained
         - Allows one null key
         
      2. HashSet<E>
         - Stores unique elements only
         - No order maintained
         - Useful for checking existence
         
      3. LinkedHashMap / LinkedHashSet
         - Maintains insertion order
         - Slightly slower than HashMap/HashSet
   
   📌 When to Use Hashing:
      ✓ Frequency counting problems
      ✓ Finding duplicates
      ✓ Two Sum, K Sum problems
      ✓ Checking existence in O(1)
      ✓ Group anagrams
   
   📌 Common Methods:
      map.put(key, value)           → Add/Update
      map.get(key)                  → Get value
      map.getOrDefault(key, def)    → Get or return default
      map.containsKey(key)          → Check if key exists
      map.keySet()                  → Get all keys
      set.add(element)              → Add element
      set.contains(element)         → Check existence
*/

